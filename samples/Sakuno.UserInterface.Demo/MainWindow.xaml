<uic:ModernWindow x:Class="Sakuno.UserInterface.Demo.MainWindow"
                  x:ClassModifier="internal"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                  xmlns:uic="clr-namespace:Sakuno.UserInterface.Controls;assembly=Sakuno.UserInterface"
                  mc:Ignorable="d"
                  Title="Demo"
                  Width="800" Height="600"
                  TextOptions.TextFormattingMode="Display">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="23" />
        </Grid.RowDefinitions>

        <Grid HorizontalAlignment="Left">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Grid.Resources>
                <ControlTemplate x:Key="ControlsSample">
                    <StackPanel>
                        <CheckBox Margin="4">
                            <TextBlock Text="CheckBox" />
                        </CheckBox>

                        <RadioButton Margin="4">
                            <TextBlock Text="RadioButton" />
                        </RadioButton>
                        <RadioButton Margin="4">
                            <TextBlock Text="RadioButton" />
                        </RadioButton>
                        <RadioButton Margin="4">
                            <TextBlock Text="RadioButton" />
                        </RadioButton>

                        <Button Margin="4">
                            <TextBlock Text="Button" />
                        </Button>
                    </StackPanel>
                </ControlTemplate>
            </Grid.Resources>

            <Control Template="{StaticResource ControlsSample}" />
            <Control Template="{StaticResource ControlsSample}" IsEnabled="False" Margin="4, 0" Grid.Column="1" />
        </Grid>

        <Rectangle Fill="{DynamicResource AccentBrushKey}" Grid.Row="1" />
    </Grid>
</uic:ModernWindow>
